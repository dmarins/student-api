{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "local debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceRoot}/cmd",
            "cwd": "${workspaceRoot}",
            "env": {
                "APP_ENV": "local"
            },
            "preLaunchTask": "Make local-restart",
            "showLog": true,
        }
    ]
}